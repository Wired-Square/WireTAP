# Capture database schema migrations

Numbered, run-once SQL migrations for `buffers.db`, applied by
`run_migrations` in `src-tauri/src/capture_db.rs`.

**Read [docs/capture-db-migrations.md](../../docs/capture-db-migrations.md)
before adding a file here.** Never edit or renumber a shipped migration.
